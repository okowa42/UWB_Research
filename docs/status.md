<!--
フォーマット規約:
- 「現在の状態」「次にやること」は常に上書きで最新化(古い内容は残さない)
- 「セッションログ」は新しい日付を上に追記。各回3行以内
- Cowork(Windows側AI)が読むファイル。実装を知らない読者にも状態が伝わる書き方をする
-->
---
last_updated: 2026-07-16
phase: 案B Phase B 追補①〜④ 完了 + Phase C 着手（一意性検査・E3 2D校正, E0全緑11）
---
# 実装状態(Cowork連携用)

## 現在の状態
- 案B（月面UWB自己校正）: **Phase A・B・追補①〜④に加え Phase C（一意性検査・E3）に着手**。`selfcal/` パッケージ。
- **E0 受け入れテスト 全緑**（`pytest tests/` = 11 passed）。V-9（多スタート一意性）を追加。metrics に
  `coverage_100mm` と `delta_rmse_tag_h/v_mm`（タグ側誤差の水平/鉛直分解）を追加しても V-1〜V-8 は不変。

### Phase C 着手（一意性検査・E3 2D校正）
- **多スタート大域一意性検査（`uniqueness.py`, V-9）**: 剛性ランクは局所一意性の必要条件だが鏡映等の
  離散不定性は捕捉できない（§4.4 注意1）。複数初期値から自己校正し低残差解を回転のみ Procrustes で
  クラスタリング、相異なる解≥2 なら「離散不定性あり」と判定。**三脚（頂点を非共線3点に距離拘束）は
  剛性ランク充足でも頂点反転の2解を検出**＝一意性検査が剛性検査の原理的死角を補う。既知非共面(E1)は1解。
- **E3 2D校正モード（`--exp E3`, dof=2）**: 全アンカー z を意図値固定で x,y のみ推定。近共面E1で
  **アンカー鉛直 abs 2001→95mm / shape 1128→72mm と劇的に安定化**。ただし**タグ鉛直測位は 3078→6194mm
  と悪化**（z固定でアンカーが一層共面化し VDOP 悪化）。⇒ 2D校正はアンカー自己校正を救うがタグ測位の
  律速（近共面 VDOP）は救えず＝高さ多様性が本質、を再補強。

### 追補④ N_a スイープ補完（欠けていた 5/10/16 を追加）
- N_a=[4,5,6,8,9,10,12,16] を全水準実行。**鉛直 shape RMSE は N_a=5→16 で ≈530→1000mm と頭打ち**
  （N_a=4 は既知4台と一致し退化=0mm）。**台数を増やしても同一平面上なら鉛直は改善しない**
  → 効くのは台数でなく高さ多様性。所見2改・所見5として研究主張を補強。

### 追補③ 仰角スイープの実現可能域細分化（研究主張の要修正）
- 既知1台の仰角を 1500〜5000mm を 500mm 刻み＋参考点{10000,40000}mm でスイープ。
- **実現可能域（着陸機構体上部 〜2900mm, 上限5000mm）では鉛直 shape RMSE は 1168→1061mm（≈9%減）
  にとどまる**。大幅改善は 10000mm(564mm)/40000mm(134mm) というタワー級高さでのみ生じる。
- ⇒ 旧主張「40mで鉛直8倍改善」は**実現性の弱い理論上限**。実用域では「単一アンカーを上げる」
  だけでは不足。主張は「単一昇降では実用不足／高さ多様性は複数台の分散配置か2D校正で確保すべき」
  へ修正が必要（Coworkへの連絡事項参照）。

### 追補② E1 タグ側数値（誤差伝搬＝ギャップ3の定量化）
- E1（N_mc=100 中央値）: アンカー abs 鉛直=2001mm → **タグ RMSE_tag 鉛直=3078mm / 水平=138mm**。
  **C(200mm)=4% / C(100mm)=1%**。PDOP_est=8.8, 過信度=3.4。
- **ΔRMSE_tag_v が負（−2912mm）**: 真アンカー版タグ測位（鉛直5990mm）の方が推定版より悪い。
  近共面は VDOP≈9〜12 が測距ノイズを増幅し**幾何自体が破綻**（校正誤差の単純加算では説明不能）。
- 対照: 高さ多様40mmでは VDOP≈2, RMSE_tag_v≈246mm, **ΔRMSE_tag_v≈+3mm** と正常化。
  ⇒ タグ側鉛直破綻の主因は「アンカー2001mm誤差の伝搬」でなく「近共面 VDOP による幾何律速」。

### 追補① 破綻領域マップ（2種の破綻を区別・色分け）
- **剛性破綻マップ（N_a×R_max, タグOFF）**: **R_max≲70m で全域 rigidity_ok=False**（校正不能）。
  周長配置の対角≈141m が閾で R_max≥110m で剛性回復。N_a(5〜12) には非依存＝R_max のエッジ欠測が支配。
- **精度破綻マップ（σ_deploy×σ_r, タグON, C(200mm)<95%）**: 近共面基底では**全30セルで C(200mm)<10%**
  （σ_r が支配 10%→1%, σ_deploy 100→10000mm はほぼ無影響）＝幾何律速で全域破綻。
- `make_figures.py` が ×赤(剛性)/△橙(精度) を凡例付きで重畳描画。図: `results/e2_grid_rigidity.png`,
  `e2_grid.png`(coverage), `e2_height_v.png`, `e2_n_anchors_v.png`。
- profiler: 剛性グリッドは悪条件LMで max≈3.0s/trial（median 11ms）、精度グリッドは タグ測位で 377ms/trial。

## 次にやること
- [ ] Phase C 深掘り: E3 を多条件スイープ化（2D vs 3D の σ_r/N_a/高さ多様性比較）、一意性検査を
      MC ループへ組み込み条件別の離散不定性発生率を集計。
- [ ] （T-008 上田教授相談後）推定器比較・PF 追加。TBD-3 Nüchter 数値照合。
- [ ] 研究主張の文言修正（所見2改・所見3・E3）を仕様書/論文ドラフトへ反映（Cowork 判断待ち）。

## Coworkへの連絡事項
- **研究主張の要修正（重要）**: 追補③の細分化で「既知1台を実現可能域(〜5000mm)まで上げても鉛直
  自己校正は≈9%しか改善しない」ことが判明。旧「40mで8倍改善」は理論上限であり、ゼミ突込み対策
  として**主張を「単一アンカー昇降では実用不足→高さ多様性は複数台分散か2D校正で確保」へ改める**
  ことを提案。所見5（N_a頭打ち）も同方向。仕様書§5〜7 の範囲内だが主張の言い回しは要調整。
- **図の研究フォルダ共有は保留**: CLAUDE.md 安全規則で /mnt/c への書き込みは status ミラーのみ許可。
  図(PNG)を `Claude用参考資料/selfcal_figs/` へ自動コピーするのは規則違反のため未実施。
  ユーザー承認で例外化するか、灯が手動で `selfcal/results/*.png` を取り出す運用を希望。
- TBD 残（実装ブロッカーでない）: TBD-3 Nüchter再現数値、TBD-1 PF追加（T-008 相談待ち）。

## セッションログ
### 2026-07-16
- Phase C 着手: 一意性検査(uniqueness.py, V-9)実装。三脚flipで剛性充足でも離散不定性検出。E3 2D校正追加。
  E3で アンカー鉛直2001→95mm安定化もタグ鉛直3078→6194mm悪化=VDOP律速を再確認。E0 11 passed。
- Phase B 追補①〜④ を実装。E0 9→維持。破綻マップ2種・タグ側誤差伝搬・仰角細分・N_a補完(2ec84ca)。
- 追補③で研究主張を要修正と判明: 実現可能域(〜5000mm)の鉛直改善は≈9%止まり(旧40m/8倍は理論上限)。
- 追補②: 近共面はタグ側も VDOP≈9〜12 で幾何破綻(ΔRMSE_tag_v 負), C(200mm)=4%。
- Phase B 実装: E2スイープ・make_figures・V-4/V-5 追加。E1所見をN_mc=100中央値で裏取り(41114f8,04c16f9)。
### 2026-07-13
- 仕様書 v1.1 を再読し実装計画を確定。σ_v150/非共面G1/G2規約固定/H-V分解/R_max150m/2D校正/V-8 を反映。
- 案B実装依頼を受領。仕様書・NotebookLM①〜④・既存資産を精読し実装計画書を作成。
- Cowork連携セットアップ(フェーズ1〜4)完了・push済み。CLAUDE.mdをインデックス化しrules/分割。
