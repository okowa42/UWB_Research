# selfcal 実行環境ピン(2026-09-14 確定)
#
# 版を固定する理由: numpy/scipy/BLAS が変わると scipy.least_squares の収束経路が
# 変わりうるため。実際に、同じ実験の再実行で個別試行の値が全行変わった例がある
# (2026-09-14。分布はほぼ一致。原因は未特定で、環境差は有力候補の1つ)。
# CSV を突き合わせる際は、まず meta.json で版が揃っているかを確認すること。
#
# 2026-07 の WSL Ubuntu-22.04 実行環境に合わせてある(python のみ 3.10 → 3.12)。
numpy==2.2.6
scipy==1.15.3
matplotlib==3.10.9
PyYAML==6.0.3
pytest==9.1.1
